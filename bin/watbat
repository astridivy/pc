#!/bin/bash
#
# watbat
#
# watch battery
#
# astrid
# 2022-12-06
#

CMD="watch -n 2 -w -t -c -d -x batt"

alacritty --config-file ~/.config/alacritty/bitsy.toml --title "+b-" -e $CMD ||
  xterm -geometry 18x1 -e $CMD
