#!/bin/bash
# cat_or_ls
#
# just try your best
#
# astridivy
# 2019-01-17

. ~/.bashrc/flags

if [[ -f $1 ]]; then
  cat "$@";
elif [[ -d $1 ]]; then
  ls "$1"
else
  echo "no such thing as '$@'"
  exit 1
fi
