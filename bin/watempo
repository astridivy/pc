#!/bin/bash
#
# watempo
#
# pick up the tempo !
#
# astrid
# 2022-12-25
#

CMD="taptempo"

alacritty --config-file ~/.config/alacritty/eensy.toml --title "tmpo" -e $CMD ||
  xterm -geometry 36x8 -e $CMD
